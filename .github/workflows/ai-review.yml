name: AI Continuous Evolution

on:
  schedule:
    # ၁ နာရီခြားတစ်ခါ အလိုအလျောက် run စေရန် (UTC အချိန်ဖြင့်)
    - cron: '0 * * * *'
  workflow_dispatch: # Manual နှိပ်ပြီး ခိုင်းချင်ရင်လည်း ရအောင် ထည့်ထားပေးပါသည်

jobs:
  ai-evolution:
    runs-on: ubuntu-latest
    # PR ဆောက်နိုင်ရန်နှင့် code ရေးနိုင်ရန် permission ပေးခြင်း
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Initialize and Install Gemini Library
        run: |
          npm init -y
          npm install @google/generative-ai

      - name: Run AI Agent
        run: node ai-review.js
        env:
          # GitHub Secrets ထဲက Key ကို အသုံးပြုပါသည်
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Create Evolution Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "AI Evolution: Hourly UI & Code Improvements"
          branch: ai-hourly-evolution
          title: "🤖 AI Automated Evolution"
          body: |
            Gemini AI က မင်းရဲ့ App ကို ၁ နာရီခြားတစ်ခါ စစ်ဆေးပြီး ပိုကောင်းအောင် ပြင်ဆင်ပေးထားပါသည်။
            - UI/UX အဆင့်မြှင့်တင်ခြင်း
            - Code သန့်ရှင်းရေးလုပ်ခြင်း
            - Responsive ဖြစ်အောင် ပြုပြင်ခြင်း
          delete-branch: true
